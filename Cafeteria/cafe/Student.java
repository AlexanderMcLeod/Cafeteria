package cafe;

// Inherits the Customer class without changing it at all.
// Does not allow the user to set whether the customer can push in the line because they cannot
public class Student extends Customer {}
